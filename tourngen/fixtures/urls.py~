from django.conf.urls import patterns, include, url

urlpatterns = patterns ('',
url(r'^create/$' , 'fixtures.views.create'),
url(r'^all/$' , 'fixtures.views.fixtures'),  
url(r'^get/(?P<team_id>\d+)/$', 'fixtures.views.fixture'),
)
